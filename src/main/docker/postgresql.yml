version: '2'
services:
  helios-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Helios/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Helios
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
